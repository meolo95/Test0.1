using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BowGoblin : MonoBehaviour
{
    // Start is called before the first frame update
    public Animator bowGoblinAnim;
    void Start()
    {
        bowGoblinAnim = GetComponent<Animator>();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
