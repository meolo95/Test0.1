using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HitByP : MonoBehaviour
{
    // Start is called before the first frame update

    public bool Shielding;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
